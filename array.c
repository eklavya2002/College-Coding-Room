#include <stdio.h>

int main()
{
    int sum=0, i, n;
    n=100;
    printf("enter the value for n\n");
    scanf("%d", &n);
    for(i=1; i<=n; i++)
    {
        if(1%2 == 0)
        {
             sum = sum+1;
             printf("the sum number " ,sum);
             scan
        }
    }
    return 0;
}